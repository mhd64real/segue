// A loading file only shows when its own folder's child changes. The dashboard one covers
// the sidebar; this one covers moving between the list, New video and a video page.
export { default } from "@/app/(dashboard)/loading";
